# Fail loudly when someone's Node or npm is older than the "engines" field in
# package.json, instead of installing anyway and quietly producing a different
# package-lock.json than everyone else has.
engine-strict=true

# Keep the lockfile honest: `npm install` should not silently drift.
save-exact=false
