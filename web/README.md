# BiletFlow web

Basic Next.js frontend. Reads fake events from `src/mocks.ts` (same shape as `docs/api.md`).

```
cd web
npm ci
npm run dev
```

Opens on http://localhost:3001 so it does not fight the API on :3000.
